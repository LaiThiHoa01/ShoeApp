<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="4dp"
    android:paddingEnd="4dp"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <!-- ================= USER MESSAGE CONTAINER (Right Aligned) ================= -->
    <LinearLayout
        android:id="@+id/layout_user"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:layout_marginStart="56dp"
        android:background="@drawable/bg_chat_bubble_user"
        android:orientation="vertical"
        android:paddingStart="14dp"
        android:paddingTop="10dp"
        android:paddingEnd="14dp"
        android:paddingBottom="10dp"
        android:elevation="1dp">

        <TextView
            android:id="@+id/text_user_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/brand_white"
            android:textSize="14sp"
            android:lineSpacingMultiplier="1.1"
            android:textIsSelectable="true"
            tools:text="User message text goes here" />
    </LinearLayout>

    <!-- ================= BOT MESSAGE CONTAINER (Left Aligned with Avatar) ================= -->
    <LinearLayout
        android:id="@+id/layout_bot"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="48dp"
        android:gravity="top"
        android:orientation="horizontal">

        <!-- Bot Avatar -->
        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_circle_orange"
            android:elevation="1dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_sole_step_mark"
                android:tint="@color/brand_white" />
        </FrameLayout>

        <!-- Bot Message Bubble & Actions Stack -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Bubble -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_chat_bubble_bot"
                android:orientation="vertical"
                android:paddingStart="14dp"
                android:paddingTop="10dp"
                android:paddingEnd="14dp"
                android:paddingBottom="10dp"
                android:elevation="1dp">

                <TextView
                    android:id="@+id/text_bot_message"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:lineSpacingMultiplier="1.1"
                    android:linksClickable="true"
                    android:textColorLink="@color/brand_orange"
                    tools:text="Bot assistant message text goes here. Product link: 'Nike Air Force 1 07' is clickable." />
            </LinearLayout>

            <!-- AI Bot Actions Bar (Feedback & Add to Cart) -->
            <LinearLayout
                android:id="@+id/layout_bot_actions"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="4dp"
                android:paddingStart="4dp">

                <!-- Thumbs Up -->
                <ImageButton
                    android:id="@+id/btn_like"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_thumb_up"
                    android:tint="@color/text_muted"
                    android:padding="6dp"
                    android:scaleType="fitCenter" />

                <!-- Thumbs Down -->
                <ImageButton
                    android:id="@+id/btn_dislike"
                    android:layout_width="28dp"
                    android:layout_height="28dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_thumb_down"
                    android:tint="@color/text_muted"
                    android:padding="6dp"
                    android:scaleType="fitCenter"
                    android:layout_marginStart="4dp" />

            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

    <!-- ================= ERROR MESSAGE CONTAINER (Left Aligned with Avatar) ================= -->
    <LinearLayout
        android:id="@+id/layout_error"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="48dp"
        android:gravity="top"
        android:orientation="horizontal">

        <!-- Bot Avatar -->
        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_circle_orange"
            android:elevation="1dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_sole_step_mark"
                android:tint="@color/brand_white" />
        </FrameLayout>

        <!-- Error Bubble + Retry Stack -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Red Error Bubble -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_chat_bubble_error"
                android:orientation="vertical"
                android:paddingStart="14dp"
                android:paddingTop="10dp"
                android:paddingEnd="14dp"
                android:paddingBottom="10dp"
                android:elevation="1dp">

                <TextView
                    android:id="@+id/text_error_message"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="#DC2626"
                    android:textSize="13sp"
                    android:lineSpacingMultiplier="1.1"
                    tools:text="API call failed: 503 Service Unavailable" />
            </LinearLayout>

            <!-- Retry button -->
            <TextView
                android:id="@+id/btn_retry"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:layout_marginStart="4dp"
                android:text="Thử lại ↺"
                android:textColor="@color/brand_orange"
                android:textSize="12sp"
                android:textStyle="bold"
                android:clickable="true"
                android:focusable="true"
                android:padding="4dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- ================= LOADING MESSAGE CONTAINER (Left Aligned with Avatar) ================= -->
    <LinearLayout
        android:id="@+id/layout_loading"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginEnd="48dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <!-- Bot Avatar -->
        <FrameLayout
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginEnd="8dp"
            android:background="@drawable/bg_circle_orange"
            android:elevation="1dp">

            <ImageView
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_sole_step_mark"
                android:tint="@color/brand_white" />
        </FrameLayout>

        <!-- Loading Bubble -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_chat_bubble_bot"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingTop="10dp"
            android:paddingEnd="16dp"
            android:paddingBottom="10dp"
            android:elevation="1dp">

            <TextView
                android:id="@+id/text_loading_dots"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="SoleStep AI đang nhập..."
                android:textColor="@color/text_secondary"
                android:textSize="13sp"
                android:textStyle="italic" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
