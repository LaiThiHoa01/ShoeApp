<?xml version="1.0" encoding="utf-8"?>
<!-- Status badge — Delivered (xanh lá) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/status_success_bg_light" />
    <corners android:radius="@dimen/radius_pill" />
    <stroke android:width="@dimen/stroke_thin" android:color="@color/status_success" />
</shape>
